# Copy to .env.local (local) and set the same keys in Vercel → Project → Settings → Environment Variables.
# Strapi base = HTTPS origin only (no trailing /api).

# Server-side (preferred for Strapi: not exposed in the browser bundle)
# STRAPI_URL=https://policemart-backend.onrender.com
# STRAPI_USE=remote
# STRAPI_URL_REMOTE=https://policemart-backend.onrender.com
# STRAPI_URL_LOCAL=http://localhost:10000

# Required for next/image on Vercel: set on Production (and at build time) so next.config can whitelist
# the hostname of every absolute image URL Strapi returns (same origin as STRAPI_URL unless you use S3).
# NEXT_PUBLIC_STRAPI_URL=https://policemart-backend.onrender.com

# Optional: comma-separated hostnames when media is on S3/CloudFront/R2 (for next/image remotePatterns at build time).
# NEXT_PUBLIC_MEDIA_HOSTS=your-bucket.s3.us-east-1.amazonaws.com

# STRAPI_API_TOKEN=
